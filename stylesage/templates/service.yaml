apiVersion: v1
kind: Service
metadata:
  name: {{ include "stylesage.fullname" . }}
  labels:
    {{- include "stylesage.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stylesage.selectorLabels" . | nindent 4 }}
