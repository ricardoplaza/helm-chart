apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stylesage.fullname" . }}-test-connection"
  labels:
    {{- include "stylesage.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "stylesage.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
